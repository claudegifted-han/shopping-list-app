# 이 저장소는 공개(public)입니다. 실제 학생 제안서와 과제 카드는 커밋하지 않습니다.
# 카드에는 학생 이름·학년, 교과별 판정, 결재 기록, 원본 제안서(source/)가 들어갑니다.
# 저장소를 비공개로 바꾼 뒤 카드를 기록으로 남기고 싶을 때만 아래 규칙을 지우세요.

# 과제 카드 — 폴더만 남기고 내용은 올리지 않음
cards/*
!cards/.gitkeep

# 제안서 — 시험용 가상 제안서(예시_*)만 올림
proposals/*
!proposals/예시_*
